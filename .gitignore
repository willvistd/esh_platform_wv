# 의존성
node_modules/

# 업로드된 파일 (로컬 테스트용)
uploads/

# 환경변수 파일 (절대 git에 올리면 안 됨!)
.env
.env.local
.env.production

# 로그
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# 빌드 산출물
dist/
build/

# 임시 파일
*.tmp
*.bak

# Puppeteer가 다운로드하는 Chrome (로컬에만)
.cache/

# 배포 불필요 — 회사 참고문서·발표자료 (용량만 차지)
public/참고자료/
*.hwp
*.hwpx
*.pptx
